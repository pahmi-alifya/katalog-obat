import React from "react";
import { StyleSheet, Text, View } from "react-native";

const MedicineDetailScreen = () => {
  return (
    <View>
      <Text>Konten Detail</Text>
    </View>
  );
};

export default MedicineDetailScreen;

const styles = StyleSheet.create({});
